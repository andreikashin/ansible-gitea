# Managed by Ansible (role: jenkins_agent).
[Unit]
Description=Docker GC for the Jenkins build agent
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
EnvironmentFile={{ jenkins_agent_gc_env_file }}
ExecStart={{ jenkins_agent_gc_script }}
